namespace ConsoleHost
{
    internal struct MessageInfo
    {
        internal string message;
        internal int offset;
    }
}

////Debug.Assert(cryptMessage.offset == AssertTester.offset);
////Debug.Assert(cryptMessage.message != null);
